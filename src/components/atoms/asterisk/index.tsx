import { Typography } from '@mui/material';
import React from 'react';

const Asterisk = () => {
  return (
    <Typography component="span" color="#F23030">
      *
    </Typography>
  );
};

export default Asterisk;
